Synthetic fixtures for document parsing provider tests.

- `expected_gold.json` defines the semantic gold checks used by the benchmark.
- `source_table.csv` mirrors the table content in a simple CSV form.
- `mineru_output/` contains a fixed MinerU-compatible output bundle used to
  validate bundle discovery and normalization.

These fixtures are synthetic and deterministic. They do not claim live MinerU
accuracy.
