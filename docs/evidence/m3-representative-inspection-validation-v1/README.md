# M3 representative inspection validation evidence v1

This package was generated by the repository evidence runner through production
Session, PR-BD, PR-BF, PR-BG, and the `scientific_agent_trajectory_inspection.v1`
GET route. Machine evidence is **complete** (8 executed / 8 passed). The
immutable machine manifest and case records retain their generated
`pending` / `not_yet_claimed` review state; those records are not rewritten
after generation. The separate `owner_review.json` is **approved**, so the
verifier computes the committed package as M3 `V`-eligible.

This `V` is limited to representative runtime validation of M3 trajectory
integrity, deterministic audit, failure attribution, and read-only inspection.
It does not claim scientific performance, attribution accuracy on an M4
benchmark, or experimental validation.

Only redacted inspection responses and digests are committed. Raw Session trees,
observer publications, private locators, infrastructure values, and local paths
remain outside Git. Representative cases are not remote-backend, experimental,
high-fidelity-computation, attribution-accuracy, or M4 benchmark evidence.
